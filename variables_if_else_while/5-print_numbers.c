#include <stdio.h>

/**
 * main - prints alphabet in lower case
 *
 * Return: 0 always
 */
int main(void)
{
	int i = 0;

	while (i <= 9)
	{
		printf("%i", i);
		i++;
	}

	putchar ('\n');
	return (0);
}
